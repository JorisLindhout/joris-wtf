# Public identifiers. Copy to web/.env — required to load the site.
# The app does not ship local fallback content.
# PUBLIC_GOOGLE_ANALYTICS_ID is inlined at build time (not a Cloudflare runtime secret).

PUBLIC_SANITY_PROJECT_ID=elkf1eqd
PUBLIC_SANITY_DATASET=production
PUBLIC_GOOGLE_ANALYTICS_ID=G-PLYJRR9DPQ
