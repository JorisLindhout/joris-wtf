# Fingerprinted Astro output (CSS, JS, fonts). Cloudflare's default is
# max-age=0, must-revalidate, which would revalidate the font on every visit.
/_astro/*
  Cache-Control: public, max-age=31536000, immutable
