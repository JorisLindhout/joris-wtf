# No env vars are loaded at the repo root.
# Copy the app examples instead:
#   web/.env.example    → web/.env
#   studio/.env.example → studio/.env
# Project ID and dataset are public. Do not put API tokens here.
